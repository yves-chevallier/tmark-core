# TMark workspace. One crate per responsibility; see design/01-architecture.md
# for the dependency graph. Nothing points upward.
[workspace]
resolver = "2"
members = [
    "crates/tmark-markdown",
    "crates/tmark-ir",
    "crates/tmark-syntax",
    "crates/tmark-registry",
    "crates/tmark-fmt",
    "crates/tmark-lint",
    "crates/tmark-writers",
    "crates/tmark",
    "crates/tmark-cli",
    "crates/tmark-lsp",
    "crates/tmark-py",
    "crates/tmark-wasm",
]

[workspace.package]
version = "0.3.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/yves-chevallier/tmark-core"
rust-version = "1.80"

[workspace.dependencies]
# Core (allowed everywhere; see design/01-architecture.md §Dependencies policy)
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] } # Value keeps struct field order: fixtures and structural_json
schemars = "0.8"
# `tmark.toml` (facade; parsing only, the edges read the file)
toml = "0.8"
# Edges only
clap = { version = "4", features = ["derive"] }
lsp-server = "0.7"
lsp-types = "0.97"
crossbeam-channel = "0.5"
pyo3 = { version = "0.22", features = ["extension-module"] }
wasm-bindgen = "0.2"
# Tests
insta = "1"
proptest = "1"
